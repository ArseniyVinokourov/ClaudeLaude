# README showcase — shot list

PNGs to capture from a real Telegram client (mobile width preferred) for the
README "See it" section. Drop files here with these exact names; the README
image refs are wired to them.

| file | what to show |
|------|--------------|
| `session.png`  | a session topic mid-turn — your message, the live status line, an assistant reply with a tool call |
| `modes.png`    | the mode picker open (the six presets with the current one marked) |
| `permission.png` | a permission request with the inline Allow / Deny buttons |
| `settings.png` | the /settings menu (Whisper model · upload alert · cleanup TTL) |
| `mirror.png`   | a /bot-mirror topic streaming a terminal session |
| `tour.png`     | the guided tour, screen 0 (👋 Start here) with Next/Close |

Keep them reasonably small (resize to ~600px wide). Plain PNG, no GIF.
